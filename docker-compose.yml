version: "3.9"

services:
  memory-layer:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - db_data:/app/db
    restart: unless-stopped

volumes:
  db_data:
